services:
  - type: web
    name: dall-backend
    runtime: node
    buildCommand: |
      pip install yt-dlp
      npm install
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production