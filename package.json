{
  "name": "dall-backend",
  "version": "1.0.0",
  "description": "Real video downloader backend",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
     "postinstall": "pip install -r requirements.txt || python3 -m pip install -r requirements.txt || python -m pip install -r -r requirements.txt",
    "build": "echo 'yt-dlp installed via postinstall'"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.9.0",
    "fluent-ffmpeg": "^2.1.2",
    "fs-extra": "^11.1.1",
    "helmet": "^7.0.0",
    "morgan": "^1.10.0",
    "multer": "^2.1.1",
    "redis": "^4.6.7",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}